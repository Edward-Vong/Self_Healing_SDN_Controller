1. Test basic traffic. 
    - checking for avg packet delay and resource usage
2. Test when the controller starts to drop packets with all mitigation logic turned off
    - ( device dependent but gives baseline for testing )
    - how long does it take to respond to packets from another switch, if at all?
    - how much load is the device on?
3. Test the mitigation logic turned on
    - how long to respond to switch?
    - at what point does the mitigation logic turn on? 
    - need to measure what state it turned into at what time. 